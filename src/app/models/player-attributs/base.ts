export class Base {
  constructor(public Lepmax: number, public AspMax: number, public KapMax: number, public Sk: number, public Zk: number, public Aw: number, public Ini: number, public Ges: number) {
  }

}
