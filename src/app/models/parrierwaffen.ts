export class Parrierwaffen{
  constructor(public id:string,public waffe: number, public str: number, public bf: number, public AT_PAMOD1:number, public AT_PAMOD2: number, public gewicht: number) {
  }
}
